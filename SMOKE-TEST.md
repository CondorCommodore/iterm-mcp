# Restart-Survival Smoke Test

This smoke test captures the restart-survival scenario where a task was tab-dispatched, Control Room was killed mid-execution, Codex kept working in the persistent iTerm tab, the watcher rehydrated the running task after Control Room came back, and the PR still landed successfully.
