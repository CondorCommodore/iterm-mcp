{
  "name": "iterm-mcp",
  "version": "0.2.0",
  "description": "MCP server for iTerm2 tab control — conductor pattern primitives",
  "type": "module",
  "bin": {
    "iterm-mcp": "dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run tests/unit",
    "test:watch": "vitest tests/unit",
    "test:smoke": "vitest run tests/integration",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
